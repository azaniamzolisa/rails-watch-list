<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :movie_id, collection: Movie.all, label_method: :title, label: 'Movie', include_blank: false %>
  <%= f.input :comment %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
